"""
Answer synthesis domain.

Empty for now: the synthetic answer is shaped as a Pydantic
`QueryResponse` in `bodhi_rag.application.models` (the public API
contract). If/when the answering context grows invariants (answer
length caps, refusal policies, source-grounded templates), the
domain entities and value objects that express them belong here.
"""
